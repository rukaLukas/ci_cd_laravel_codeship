- name: prepare
  service: php
  command: composer install --no-interaction
  command: ls -l

- name: tests
  service: php
  command: php ./vendor/bin/phpunit

- name: static Analysis
  tag: main
  service: php
  command: php -v
